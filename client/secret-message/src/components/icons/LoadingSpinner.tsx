import "ldrs/ring";
const loadingSpinner = () => {
  //just a disclaimer this always returns an error even tho it works perfectly fine, I honestly just don't know how to fix it trust me I've tried my hardest
  return <l-ring color="var(--color-secondary)" size={31}></l-ring>;
};

export default loadingSpinner;
